<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.infrastructure.mybatis.mapper.PasswordResetTokenMapper">
    <insert id="insert">
        insert into password_reset_tokens (id, token, user_id, expires_at, used) values(
          #{passwordResetToken.id},
          #{passwordResetToken.token},
          #{passwordResetToken.userId},
          #{passwordResetToken.expiresAt},
          #{passwordResetToken.used}
        )
    </insert>
    <update id="update">
        update password_reset_tokens
        set expires_at = #{passwordResetToken.expiresAt},
            used = #{passwordResetToken.used}
        where id = #{passwordResetToken.id}
    </update>
    <select id="findById" resultMap="passwordResetToken">
        select id, token, user_id, expires_at, used from password_reset_tokens where id = #{id}
    </select>
    <select id="findByToken" resultMap="passwordResetToken">
        select id, token, user_id, expires_at, used from password_reset_tokens where token = #{token}
    </select>

    <resultMap id="passwordResetToken" type="io.spring.core.user.PasswordResetToken">
        <id column="id" property="id"/>
        <result column="token" property="token"/>
        <result column="user_id" property="userId"/>
        <result column="expires_at" property="expiresAt"/>
        <result column="used" property="used"/>
    </resultMap>
</mapper>
