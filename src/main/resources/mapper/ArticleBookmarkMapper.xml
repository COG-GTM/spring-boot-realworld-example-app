<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.spring.infrastructure.mybatis.mapper.ArticleBookmarkMapper">

    <select id="find" resultType="io.spring.core.bookmark.ArticleBookmark">
        select article_id articleId, user_id userId, created_at createdAt
        from article_bookmarks
        where article_id = #{articleId} and user_id = #{userId}
    </select>

    <insert id="insert">
        insert into article_bookmarks (article_id, user_id, created_at)
        values (#{bookmark.articleId}, #{bookmark.userId}, #{bookmark.createdAt})
    </insert>

    <delete id="delete">
        delete from article_bookmarks
        where article_id = #{bookmark.articleId} and user_id = #{bookmark.userId}
    </delete>

    <select id="findByUserId" resultType="string">
        select article_id
        from article_bookmarks
        where user_id = #{userId}
        order by created_at desc
        limit #{limit} offset #{offset}
    </select>

    <select id="countByUserId" resultType="int">
        select count(*) from article_bookmarks where user_id = #{userId}
    </select>

</mapper>
